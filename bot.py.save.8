from telegram import Update
from telegram.ext import Application, CommandHandler, MessageHandler, filters, ContextTypes

TOKEN = "7872471977:AAGu71rkVdC4FOjor95J_b6uBlvTlk88l1Y"

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        "👋 سلام! به **رزومه‌ساز هوشمند** خوش اومدی!\n\n"
        "اطلاعاتت رو بفرست (تحصیلات، تجربیات، مهارت‌ها، شغل مورد نظر)\n"
        "من برات رزومه حرفه‌ای می‌سازم."
    )

async def make_resume(update: Update, context: ContextTypes.DEFAULT_TYPE):
    user_input = update.message.text
    await update.message.reply_text("⏳ در حال ساخت رزومه...")

    resume = f"""
📄 **رزومه حرفه‌ای شما**

{user_input}

---
✅ این رزومه توسط هوش مصنوعی ساخته شد.
برای نسخه بهتر، اطلاعات بیشتری بده!
    """
    await update.message.reply_text(resume)

# راه‌اندازی بات
app = Application.builder().token(TOKEN).build()

app.add_handler(CommandHandler("start", start))
app.add_handler(MessageHandler(filters.TEXT & \~filters.COMMAND, make_resume))

print("✅ بات رزومه‌ساز هوشمند با موفقیت شروع شد!")
app.run_polling()
